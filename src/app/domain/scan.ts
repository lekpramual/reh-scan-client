export class Scan {
  userId!: number;
  checkType!: number;
  scanId!: string;
}

// const data = {
  //   userId: Auth.getProfile().id,
  //   checkType: msg === "chkin" ? "0" : "1",
  //   scanId: location
  // };